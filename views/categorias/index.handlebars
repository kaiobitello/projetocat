<style>
@media (max-width:700px){
 .card-body {
  width: 100%;
  max-width: 300px;
  background: #fff;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  margin: 1rem;
}

.card-body img {
  width: 100%;
  height: auto;
  object-fit: cover;
  display: block;
}

.card-body {
  padding: 1rem;
  font-family: sans-serif;
}

.card-body h2 {
  font-size: 1.2rem;
  margin: 0 0 0.5rem;
}

.card-body p {
  color: #333;
  font-size: 0.95rem;
}
}
</style>

<h1>Categorias</h1>
<hr>
{{#each categorias}}
<div class="card mt-3">
    <div class="card-body">
        <a href="/categorias/{{slug}}" style="text-decoration:none"><h4>{{nome}}</h4></a>
        <small>Slug: {{slug}}</small><br>
    </div>
</div>
{{/each}}